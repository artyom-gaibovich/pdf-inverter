## 1. Каркас проекта

- [x] 1.1 Инициализировать проект React + Vite (структура `src/`, `index.html`) и убедиться, что `npm run dev` запускает пустое приложение
- [x] 1.2 Добавить зависимости `pdfjs-dist` и `pdf-lib` в `package.json` и проверить, что `npm ci` проходит без ошибок
- [x] 1.3 Подключить воркер PDF.js как статический ассет с учётом `import.meta.env.BASE_URL` и убедиться, что воркер загружается в dev-режиме без ошибок в консоли

## 2. Загрузка и парсинг PDF

- [x] 2.1 Реализовать компонент загрузки файла (input/drag-and-drop) и проверить, что выбранный `.pdf` читается в память браузера
- [x] 2.2 Открывать документ через PDF.js, определять `numPages` и отображать число страниц в UI — проверить на тестовом многостраничном PDF
- [x] 2.3 Обрабатывать некорректный/битый файл: показывать сообщение об ошибке и не начинать обработку — проверить на не-PDF файле

## 3. Диапазон страниц (нарезка)

- [x] 3.1 Добавить в UI необязательные поля «с» и «по» для диапазона страниц и проверить, что пустой диапазон означает весь документ
- [x] 3.2 Реализовать валидацию диапазона (1 ≤ N ≤ M ≤ numPages, числовой ввод) с сообщениями об ошибке — проверить кейсы: начало>конца, выход за пределы, нечисловой ввод
- [x] 3.3 Ограничить обработку выбранным диапазоном N..M включительно и проверить, что в результат попадают только эти страницы

## 4. Инверсия и рендер

- [x] 4.1 Рендерить каждую страницу диапазона на `<canvas>` через PDF.js со scale ≥ 2 и проверить читаемость растра
- [x] 4.2 Реализовать попиксельную инверсию `ImageData` (255 − RGB, alpha без изменений) и проверить: белый фон → чёрный, чёрный текст → белый
- [x] 4.3 Освобождать canvas/ImageData между страницами и проверить обработку многостраничного файла без роста памяти до отказа

## 5. Экспорт PDF

- [x] 5.1 Экспортировать инвертированный canvas в изображение (JPEG) и встраивать в новый PDF через `pdf-lib` с сохранением исходных пропорций страницы
- [x] 5.2 Собирать итоговый PDF из всех обработанных страниц и инициировать скачивание — проверить, что открытый файл содержит инвертированные страницы выбранного диапазона
- [x] 5.3 Показывать индикатор прогресса во время обработки и завершать его по готовности файла — проверить на многостраничном PDF

## 6. Деплой на GitHub Pages

- [x] 6.1 Задать `base: '/<repo>/'` в `vite.config` и проверить, что `npm run build` формирует статику, работающую из подкаталога (локальный preview по базовому пути)
- [x] 6.2 Добавить GitHub Actions workflow (build → `upload-pages-artifact` → `deploy-pages`) с триггером на push в основную ветку и проверить успешный прогон
- [x] 6.3 Задокументировать в README включение Pages (source = GitHub Actions) и известное ограничение (растровый результат без текстового слоя); проверить, что приложение открывается по публичному URL и выполняет полный цикл: загрузка → нарезка → инверсия → скачивание
